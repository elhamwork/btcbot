data/raw/
data/processed/
__pycache__/
*.pyc
.env
venv/
.venv/
*.egg-info/
.DS_Store

# Output of fetch_real_data.py / fetch_btc_prices.py.
# Deliberately NOT ignored: this is the real market data the backtest needs,
# and committing it is the simplest way to move it from the machine that can
# reach Kalshi to the one running the analysis. It is public market data.
# !real_data/

# forward test output
forward_test/
